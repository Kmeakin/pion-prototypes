def app0 =
    let f = fun() => false;
    f();

def app1 =
    let f = fun(x: Int) => x;
    f(1);

def curried = Array(Int);

def error1 =
    let f = fun(x: Int) => x;
    f();

def error2 = Int();

def error3 = Array(@Int);

def error4 = Array(Int, 0, 0);
