def tuple0 = ();
def tuple1 = (1,);
def tuple2 = (1,2);
def tuple3 = (1,2,3);

def pair_make = fun(A: Type, B: Type, x: A, y: B) => (x, y);
def pair_id   = fun(A: Type, B: Type, p: (A, B)) => (p._0, p._1);
def pair_swap = fun(A: Type, B: Type, p: (A, B)) => (p._1, p._0);
